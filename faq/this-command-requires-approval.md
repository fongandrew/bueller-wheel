If you get `Error: This command requires approval` or similar while running a Bash call, try running it using the sandboxed version of bash.

Otherwise, try to the unsandboxed version of bash.

If neither works, check permissions for an allowed workaround command.

